[project]
name = "research_transport_mobility"
version = "0.1.0"
description = "Research pillar notebooks"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.4"]

[tool.pytest.ini_options]
testpaths = ["tests"]
